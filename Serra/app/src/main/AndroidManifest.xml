<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permesso per accedere a Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Aggiungere i permessi per Firebase, se usati -->
    <!-- <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" /> -->
    <!-- <permission android:name="com.esempio.serra.permission.C2D_MESSAGE" android:protectionLevel="signature" /> -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/serra"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/serra_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Serra"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Serra">

            <!-- Intent filter per indicare che MainActivity è il launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Se hai bisogno di altre attività o servizi Firebase aggiungili qui -->
        <!-- <service android:name=".MyFirebaseMessagingService" android:exported="false" /> -->

    </application>

</manifest>
